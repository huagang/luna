var editRouter = angular.module('editRouter',[]);
editRouter.controller('routerController',['$rootscope', '$scope', '$http', routerController]);

editRouter.run(['$rootScope', '$http', function($rootScope, $http){
	//定义  $rootScope内容
}]);

function routerController(){
	
}